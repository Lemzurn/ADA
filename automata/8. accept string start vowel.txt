%{
#include <stdio.h>
%}

%%
^[aeiouAEIOU][a-zA-Z]* { printf("Accepted : starts with vowel\n"); }
^[a-zA-Z]+             { printf("Rejected \n"); }
\n|.                   ; 
%%

int yywrap() { return 1; }

int main() {
    printf("Enter a string: ");
    yylex();
    return 0;
}