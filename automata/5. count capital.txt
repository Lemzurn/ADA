%{
#include <stdio.h>
int count = 0;
%}

%%
[A-Z]  { count++; } /* Only matches uppercase letters */
\n     { printf("Count the capital letters : %d\n", count); count = 0; }
.      ; /* Ignore lowercase and symbols */
%%

int yywrap() { return 1; }

int main() {
    printf("Enter a word:\n");
    yylex();
    return 0;
}